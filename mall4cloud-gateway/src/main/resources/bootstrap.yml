server:
  port: 8000
spring:
  application:
    name: @artifactId@
  cloud:
    nacos:
      discovery:
        # server-addr: ${NACOS_HOST:city.duo88.cn}:${NACOS_PORT:3987}
        server-addr: city.duo88.cn:3987
        username: nacos
        password: nacos
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        file-extension: yml
        namespace: f50860af-9263-415a-b2fe-cb1397b02bcb
        shared-configs:
          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
        username: ${spring.cloud.nacos.discovery.username}
        password: ${spring.cloud.nacos.discovery.password}
  profiles:
    active: @profiles.active@
